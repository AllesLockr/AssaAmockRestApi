server:
  port: 8067

spring:
  application:
    name: AssaAmockRestApi

  h2:
    console:
        enabled: true

  datasource:
    url: jdbc:h2:mem:assamockdb
    driverClassName: org.h2.Driver
    username: sa
    password:

    jpa:
      show-sql: true
